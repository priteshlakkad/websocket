# WebSockets in a Spring Boot Application

Simple web socket example using spring boot starter project

- `/app/user` - Message Mapping URL
- `/topic/user` - Message Broker topic for pushing messages to the UI back.
 
